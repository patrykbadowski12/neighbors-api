spring:
  application:
    name: neighbors-api
  datasource:
    url: jdbc:postgresql://localhost:5432/neighbors
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
      password: eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81
  cache:
    type: redis

server:
  port: 8082
springdoc:
  api-docs.enabled: true
  swagger-ui.path: /swagger-ui.html